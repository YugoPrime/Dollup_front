// Same string as apex's PDP_COLOR_CHANGE_EVENT so the two storefronts share
// the wire format if they ever co-exist on one origin. Re-defined here so the
// preorder route group stays self-contained.
export const PDP_COLOR_CHANGE_EVENT = "pdp:color-change";
