export function PreorderBadge() {
  return (
    <span className="inline-flex items-center rounded-full border border-sage-300 bg-sage-50 px-2 py-0.5 text-[10px] font-semibold uppercase tracking-[0.12em] text-sage-700">
      Pre-order
    </span>
  );
}
